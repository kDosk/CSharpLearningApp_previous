<Window x:Class="CSharpLearningApp.MainKamilysWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CSharpLearningApp"
        mc:Ignorable="d"
        Title="MainKamilysWindow" Height="450" Width="800" >
    <Grid>
        <StackPanel>
            <TextBlock Background="Red" FontSize="20" FontFamily="Times New Roman" TextWrapping="Wrap" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"><Italic>Тема: Переменные и константы</Italic></TextBlock>
            <TextBlock Text="Для хранения данных в программе применяются переменные.  Переменная представляет именнованную область памяти, в которой хранится значение определенного типа. 
 Переменная имеет тип, имя и значение. Тип определяет, какого рода информацию может хранить переменная.
 Перед использованием любую переменную надо определить. Синтаксис определения переменной выглядит следующим образом:" 
                       Background="White" FontSize="14" FontFamily="Times New Roman" TextWrapping="Wrap" />
            <Label Content="тип имя_переменной;" Margin="20,5,564.6,10" FontSize="20"/>
            <Ellipse Fill="Red" Width="20" Height="20" Margin="-720,-40,50,700"/>
        </StackPanel>
    </Grid>
</Window>
